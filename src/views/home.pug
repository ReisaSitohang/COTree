html
	head
		title Calculation
		include includes/head.pug
		include includes/calculationscriptrdw.pug			
	body
		include includes/navbar.pug
		main 
			div.container hellooo #{data.remainder}
				div.row
					div#forest 
						h3 omg them trees!
						-	for (var x=0; x < data.fulltree; x++)
								div.treecontainer
									div.tree
										div
										img(src='/images/tree2.png')
						div.remainderTreecontainer
							div.remainderTree
								div
								img(src='/images/tree2.png')
			

	footer.page-footer
		div.container
			div.row
				div.col-16-s12
					h5.white-text Footer content
					p.grey-text.text-lighten-4 You can use rows and colums here to organize your footer content.
		div.footer-copyright
			div.container  © 2016 Copyright COtree

script.
	let remainderFunction = function() {
		if('#{data.remainder}' == '0px') {
			console.log("no remains")
			$('.remainderTree').css('visibility', 'hidden')
		} else if ('#{data.remainder}' !== '0px') {
			$('.remainderTree').css('height', '#{data.remainder}')
		}
	}
	let noMoneyFunction = function () {
		if('#{data.donationAmount}' == 0) {
			console.log("nothing donated yet")
			$('.remainderTree').css('background-color', '#bdbdbd')
			$('.remainderTree').css('height', '100px')
		}
	}

	$(document).ready(function() {
		if ('#{data.donationAmount}' == 0) {
			noMoneyFunction()
			} else if ('#{data.donationAmount}' !== 0) {
			remainderFunction()
			}
		//$('div.remainderTree').css('height', '50%')
	})


